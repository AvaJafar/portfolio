// queries/getTimeline.ts
// Removed datoCMSClient and DatoCMS query

import { Skill } from '../types';

export async function getSkills(): Promise<Skill[]> {
  // Placeholder return to keep the app functional
  return [];
}
